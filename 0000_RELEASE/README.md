# 0000_RELEASE — 배포본 (Vercel 대상)

`index.html` = 최신 PMLE 학습 플랜 단일 파일. 이 폴더를 Vercel에 배포한다.

- 빌드/수정 후 `0000_PLAN/PMLE_18week_study_plan.html` → `0000_RELEASE/index.html`로 복사해 갱신.
- Vercel: 이 레포 연결 시 Root Directory를 `0000_RELEASE`로 지정(또는 폴더 드래그앤드롭).
- 개인 export(LINK.html)는 .gitignore로 제외됨(푸시 금지).
