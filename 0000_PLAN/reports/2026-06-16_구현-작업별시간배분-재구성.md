# 보고서 — 구현: 작업별 시간배분 재구성

- 날짜: 2026-06-16
- 작업자: Claude (AI)
- 요청: "구현 ㄱ" (계획서 v3: 작업별 소요시간 표시 + 가독성 + 실제시간 페이싱).

## 1. 결과 구조
- **과거 4주(완료·복습용) + 현재 15주 = 총 19주.** 라벨 W-4~W-1 / W1~W15. 첫 화면 활성 = 현재 **W1 = Create ML Models with BigQuery ML**.
- 무거운 코스 다주 분할: Feature Engineering·Keras·Production ML = 각 여러 주. 마이크로러닝(Intro GenAI·LLMs·MLOps GenAI + Model Evaluation)은 강의 배속으로 묶어 단축.

## 2. 일자 카드 — 작업별 시간배분 + 가독성
- 줄글(day-act) → **작업 리스트(day-tasks)**: 각 줄 `[Nm] 아이콘 작업(랩=하이퍼링크) 상태배지`.
- 헤더와 하단에 **⏱ 합계 Nm**. 2h 랩 있는 날은 **⤴ 그날만 상향** 표시(6곳).
- 예) Create ML Models 1일차: `[60m] 🧪 Getting Started with BigQuery ML ✅` …

## 3. 페이싱 규칙(반영)
- 세션 90m. 랩은 **실제 시간**(Predict Taxi Fare 60·Bracketology 60·TensorFlow Dataset API 120 등) 사용.
- 강의 모듈: **×0.7 배속** + 조밀 패킹, 복습/퀴즈 라인 폴딩(강의 압축).
- 2h+ 랩: 그날만 상향(⤴). 잔여 1세션은 **5회차(5일)로 흡수**해 자투리 주차 제거.

## 4. 링크·진행·기능 (유지)
- 모든 랩 링크 = LINK.html의 정확한 href(RULE §9). 진행배지(✅완료/⏳진행중/⬜시작전) = LINK.html JSON 기준. 과거 4주 100% 체크.
- 드로어/진행률/체크박스/툴바(💾 저장·📤내보내기·📥가져오기)/linkify 유지. 진행률 분모 = 19주×4.
- 드로어 openMat은 신규 주차(PLAN_DATA 없음) 가드 처리(무크래시). 정적 작업 리스트가 카드 칩을 대체(중복 제거).

## 5. 일정(이전 리포트 일치)
- 현재 15주(+과거 완료) → 6일 손실 반영 시 종료 **2026-10-20 ~ 10-27**(상한 11-03). +모의/시험대비 2~3주 → 11월 중순 응시 적기.

## 6. 검증
- `node --check` 통과. `/labs/` 127개 유지, 대표 랩(612209 Visitor·512769 TF Dataset) 노출, 19 섹션, 활성 패널·진행배지 확인.

## 7. 한계 / 후속
- 개별 랩 시간 일부는 **추정**(로그인 후 각 랩 페이지로 정밀 확정 권장).
- 강의 모듈은 코스 영상 세부 목록이 LINK.html에 없어 "🎬 강의 모듈" 블록으로 시간만 표기(개별 영상명 미표시).
- 드로어 퀴즈는 신규 주차에선 비어 있음(원하면 코스별 퀴즈 재작성 가능).

## 버전 / 롤백
- v14 → **v15** (`archive/PMLE_18week_study_plan__2026-06-16_v15_timeboxed-rebuild.html`). 롤백 v14.
